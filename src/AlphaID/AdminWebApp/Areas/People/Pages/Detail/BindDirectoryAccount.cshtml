@page
@model BindDirectoryAccountModel
@{
}
<h1>Bind exists account</h1>

<form method="post">
    <div class="input-group">
        <div class="input-group-prepend">
            <select name="serviceId" class="form-control">
                @foreach (var svc in this.Model.DirectoryServices)
                {
                    <option value="@svc.Id">@svc.Name</option>
                }
            </select>
        </div>
        <input name="keywords" class="form-control" placeholder="@L["Name, phonetic name, etc..."]" autofocus />
        <div class="input-group-append">
            <button type="submit" asp-page-handler="Search" class="btn btn-primary">@L["Search"]</button>
        </div>
    </div>


    <table class="table table-striped table-sm">
        <thead>
            <tr>
                <td>@L["Display Name"]</td>
                <td>@L["Name"]</td>
                <td>@L["SAM Account Name"]</td>
                <td>@L["User Principal Name"]</td>
                <td>@L["Mobile"]</td>
                <td>@L["Actions"]</td>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in this.Model.SearchItems)
            {
                <tr>
                    <td>@item.DisplayName</td>
                    <td>@item.Name</td>
                    <td>@item.SAMAccountName</td>
                    <td>@item.UserPrincipalName</td>
                    <td>@item.Mobile</td>
                    <td><button type="submit" asp-page-handler="Bind" asp-route-entryGuid="@item.ObjectGUID" class="btn btn-sm btn-primary">@L["Bind"]</button></td>
                </tr>
            }
        </tbody>
    </table>
</form>