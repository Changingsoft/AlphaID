@page
@model FindPasswordByEmailModel
@{
    ViewBag.Title = L["Find password via email"];
}

<div class="text-center mb-4">
    <a asp-area="" asp-page="/Index" class="navbar-brand navbar-brand-autodark">
        <img src="/logo.png" width="32" height="32" alt="Tabler" class="navbar-brand-image">
        @Product.Value.Name @SL["Account"]
    </a>
</div>
<div class="card card-md">
    <div class="card-body">
        <h2>请输入您注册账户时预留的电子邮件地址。</h2>
        <p>如果注册时未提供邮箱，或预留的邮箱未经确认，则无法收取重设密码的邮件。请考虑其他形式来重设密码。</p>
        <hr />
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="mb-3">
                <label asp-for="Input.Email" class="form-label"></label>
                <input asp-for="Input.Email" class="form-control" autocomplete="email" aria-required="true" />
                <span asp-validation-for="Input.Email" class="text-danger"></span>
            </div>
            <div class="mb-3">
                <label asp-for="Input.CaptchaCode" class="form-label required"></label>
                <captcha id="LoginCaptcha" user-input-id="CaptchaCode"/>
                <input asp-for="Input.CaptchaCode" id="CaptchaCode" class="form-control captchaVal"/>
            </div>
            <button type="submit" class="w-100 btn btn-lg btn-primary">发送重置密码邮件</button>
        </form>
    </div>
</div>
@section Scripts
{
    <script src="~/js/captcha.validate.js"></script>
}