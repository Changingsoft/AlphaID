@page
@model IndexModel
@{
    ViewBag.Title = Model.Organization.Name;
}

<div class="page-header">
    <div class="container-xl">
        <h1>@Model.Organization.Name</h1>
        <p>@Model.Organization.Description</p>
        <p>@Model.Organization.Domicile</p>
    </div>
</div>
<div class="page-body">
    <div class="container-xl">
        <h2>@L["Activities"]</h2>
        
    </div>
</div>
