@page
@model RequestsModel
@{
}
<table class="table">
    <thead>
    <tr>
        <th></th>
        <th></th>
    </tr>
    </thead>
    <tbody>
    @foreach (var request in Model.RealNameRequests)
    {
        <tr>
            <td>
                <a asp-page="Request/Index" asp-route-anchor="@request.Id">@request.Id</a>
            </td>
            <td>@request.PersonId</td>
        </tr>
    }
    </tbody>
</table>