@model int

<div class="card">
    <div class="card-header">
        <a asp-area="People" asp-page="/Index">@L["Peoples"]</a>
    </div>
    <div class="card-body">
        <p>@(Model) @L["Peoples"]</p>
    </div>
</div>